# day02.md..
الجزء 1: قائمة من عشر prompts ضعيفة.
# Day 02 - تحويل Prompts (التقارير والبيانات)

> العناصر السبعة المستخدمة: الدور / السياق / المهمة / الفورمات المطلوب / الجمهور المستهدف / القيود / معيار النجاح.
>
> بيانات المبيعات التجريبية المستخدمة في الاختبار:
>
> | الشهر | المنتج | المبيعات (جنيه) | عدد الطلبات | العملاء الجدد |
> |---|---|---|---|---|
> | يناير | باقة A | 45,000 | 120 | 30 |
> | يناير | باقة B | 28,000 | 80 | 15 |
> | فبراير | باقة A | 52,000 | 135 | 28 |
> | فبراير | باقة B | 31,000 | 90 | 20 |
> | مارس | باقة A | 48,000 | 125 | 22 |
> | مارس | باقة B | 35,000 | 95 | 25 |

---

## Prompt #1
**الأصلي:** اعملي dashboard للمبيعات

**الناقص:** المدخلات (مفيش بيانات ولا مصدر محدد) هو الأهم، وبعده صيغة المخرج (أداة الداشبورد؟ تصميم نصي ولا تنفيذ برمجي؟) والنطاق (إيه الـ KPIs بالظبط؟).

**النسخة الكاملة:**
```
الدور: انت محلل بيانات بتجهز أداة متابعة للفريق التجاري.
السياق: البيانات المرفقة [جدول المبيعات أعلاه] لباقتين A وB لمدة 3 شهور.
المهمة: صمّم (وصف نصي / wireframe، مش تنفيذ برمجي) Dashboard يعرض أهم KPIs: إجمالي المبيعات الشهري، نسبة النمو شهر على شهر، مقارنة الباقتين، ومعدل التحويل (طلبات/عملاء جدد).
الفورمات المطلوب: وصف كل قسم (اسم الـ widget، نوع الرسم، البيانات المعروضة) مرتب من الأهم للأقل أهمية.
الجمهور المستهدف: مدير المبيعات، بيفتح الداشبورد يوميًا لمتابعة سريعة مش تحليل عميق.
القيود: أقصى حاجة 6 widgets في الشاشة الأولى، من غير تفاصيل عن أداة التنفيذ (Excel/Power BI) لأن ده هيتحدد بعدين.
معيار النجاح: مدير المبيعات ياخد فكرة "هل الشهر كويس ولا لأ" في أول 5 ثواني من فتح الداشبورد.
```

**مخرج الأصلي (ملخص):** رد عام جدًا: "استخدم Power BI أو Excel، اعمل رسم بياني للمبيعات وجدول للعملاء، وضيف فلاتر وألوان جذابة" - من غير ترتيب أولويات ولا ربط بأي رقم فعلي من البيانات.

**مخرج المحول (ملخص):** 6 widgets مرتبة بالأهمية: (1) بطاقة إجمالي المبيعات الشهر الحالي مقابل السابق مع نسبة النمو، (2) رسم خطي لمقارنة الباقتين عبر الشهور، (3) بطاقة معدل التحويل لكل باقة، (4) جدول تفصيلي قابل للفلترة، وهكذا - كل widget موصوف بنوع الرسم والبيانات المعروضة فيه بالتحديد.

**الفرق:**
1. الأصلي نصايح عامة عن أدوات، المحول تصميم فعلي مرتب بالأولوية.
2. الأصلي مش مربوط بأعمدة حقيقية، المحول كل widget مبني على عمود/حساب محدد من الجدول.
3. الأصلي من غير حد لعدد الـ widgets فطلع عام وغير عملي، المحول التزم بـ6 widgets.

---

## Prompt #2
**الأصلي:** حللي البيانات دي

**الناقص:** الدور، المهمة المحددة (تحليل إيه بالظبط؟)، الفورمات، الجمهور المستهدف، القيود، معيار النجاح.

**النسخة الكاملة:**
```
الدور: انت محلل بيانات (Data Analyst) في شركة SaaS صغيرة.
السياق: بيانات مبيعات باقتين (A وB) لأول 3 شهور من السنة [الجدول مرفق].
المهمة: حلل اتجاه المبيعات شهر بشهر، وحدد أي نمط غير متوقع، وقارن أداء الباقتين.
الفورمات المطلوب: 3 نقاط رئيسية + جدول صغير للمقارنة بين الباقتين.
الجمهور المستهدف: مدير المنتج، من غير خلفية إحصائية عميقة، يفضل أرقام ونسب واضحة.
القيود: الرد ككل ما يتعديش 150 كلمة، من غير كود.
معيار النجاح: مدير المنتج ياخد قرار من غير ما يرجعلي يسأل توضيح.
```

**مخرج الأصلي (ملخص):** وصف عام للأعمدة، حساب الإجمالي الكلي (239,000 جنيه)، وقال إن باقة A "أعلى" من غير نسبة مئوية، مع فقرة عامة عن "أهمية تتبع الاتجاهات" من غير رقم فعلي. الطول حوالي 280 كلمة.

**مخرج المحول (ملخص):** 3 نقاط محددة: (1) باقة A نمو ثابت (45k→52k→48k، ~3%)، وباقة B نمو أسرع (28k→35k = +25%). (2) نسبة الطلبات للعميل الجديد في B أعلى - upsell أو churn محتمل يستاهل متابعة. (3) فبراير أعلى شهر لـA بس مارس فيه تراجع رغم زيادة الطلبات - يستاهل مراجعة متوسط سعر الطلب. الرد في حدود 140 كلمة + جدول مقارنة.

**الفرق:**
1. الأصلي وصفي وعام، المحول تحليلي ومحدد بالأرقام والنسب.
2. الأصلي أطول من الحد المسموح وفيه حشو، المحول التزم بـ150 كلمة والفورمات المطلوب.
3. الأصلي بلا أي قرار مقترح، المحول قدّم ملاحظة قابلة للتنفيذ لأن معيار النجاح كان محدد.

---

## Prompt #3
**الأصلي:** اكتبلي دالة تحسب نسبة النمو

**الناقص:** المدخلات (نمو من إيه لإيه؟ قيمتين ولا سلسلة؟)، اللغة/الفورمات، والقيود (قسمة على صفر).

**النسخة الكاملة:**
```
الدور: انت مطور Backend بتستخدم PHP (Laravel).
السياق: محتاج دالة تُستخدم جوه Controller، المدخل مصفوفة قيم مبيعات شهرية (زي [45000, 52000, 48000]).
المهمة: اكتب دالة calculateGrowthRate(array $values): array ترجع نسب النمو المئوية بين كل شهر واللي قبله.
الفورمات المطلوب: كود PHP بس، مع تعليق قصير فوق كل جزء مهم.
القيود: تعامل مع القيمة السابقة = صفر (يترجع null بدل قسمة على صفر)، والنتيجة تتقرب لرقمين عشريين.
معيار النجاح: تشتغل من غير error مع أي مصفوفة، حتى لو فيها صفر أو سالب.
```

**مخرج الأصلي (ملخص):** دالة بسيطة `($current - $previous) / $previous * 100` لرقمين بس، من غير معالجة قسمة على صفر، ومن غير تقريب.

**مخرج المحول (ملخص):** دالة كاملة بتلف على المصفوفة، بترجع null عند الصفر، والنتائج مقرّبة لرقمين عشريين، جاهزة للنسخ المباشر.

**الفرق:**
1. الأصلي لرقمين بس، المحول على سلسلة كاملة زي الاستخدام الفعلي.
2. الأصلي بلا حماية من القسمة على صفر، المحول عالجها.
3. الأصلي بلا تقريب، المحول التزم بالتقريب المطلوب.

---

## Prompt #4
**الأصلي:** سرّع التقرير ده

**الناقص:** السياق (مفيش تقرير مرفق أصلاً)، والنطاق (سرعة التوليد؟ سرعة القراءة؟ الطول المطلوب؟).

**النسخة الكاملة:**
```
الدور: انت محلل بيانات بتراجع تقرير قبل ما يتبعت للإدارة.
السياق: التقرير المرفق [تقرير Prompt #5] طوله ~210 كلمة، وياخد من القارئ ~3 دقائق، وفيه جمل مكررة.
المهمة: اختصره لنص طوله لغاية النص (~100-120 كلمة) من غير ما يضيع أي رقم أو توصية.
الفورمات المطلوب: نفس بنية التقرير الأصلي (ملخص تنفيذي + نقاط) بس مضغوط.
القيود: ما ينفعش يتشال أي رقم، بس ينفع تتدمج الجمل الوصفية.
معيار النجاح: القارئ ياخد نفس المعلومة والتوصية في نص الوقت.
```

**مخرج الأصلي (ملخص):** لأن مفيش تقرير مرفق، الرد إما طلب توضيح "أي تقرير؟" أو -لو افترض تقرير وهمي- طلع نصايح عامة عن "استخدم نقاط بدل فقرات" بدل تسريع تقرير فعلي.

**مخرج المحول (ملخص):** اختصار فعلي لتقرير Prompt #5 لنص الطول تقريبًا، مع المحافظة على كل الأرقام (نمو باقة B +25%، تباطؤ A في مارس)، وحذف الجمل الوصفية الزيادة فقط.

**الفرق:**
1. الأصلي محتاج توضيح أو طلع نصايح عامة، المحول نفّذ التسريع على نص حقيقي.
2. الأصلي بلا معيار واضح لمعنى "سرعة"، المحول حدد الهدف = تقليل وقت القراءة.
3. الأصلي معرّض لفقدان أرقام مهمة، المحول التزم بقيد الحفاظ على كل الأرقام.

---

## Prompt #5
**الأصلي:** اعملي تقرير شهري للإدارة

**الناقص:** الدور، السياق (مفيش بيانات ولا نطاق زمني)، الفورمات، الجمهور المستهدف، القيود، معيار النجاح.

**النسخة الكاملة:**
```
الدور: انت محلل بيانات بتجهز تقرير دوري للإدارة العليا.
السياق: البيانات المرفقة [الجدول أعلاه] لباقتين خلال يناير-مارس. التقرير هيتقرا في اجتماع مراجعة ربع سنوي.
المهمة: لخّص الأداء الشهري، وحدد أهم 2-3 مؤشرات محتاجة قرار من الإدارة.
الفورمات المطلوب: عنوان + ملخص تنفيذي في 3 أسطر + نقاط تفصيلية مرقمة.
الجمهور المستهدف: مدير عام مش متخصص، مشغول، محتاج التوصية من الأول.
القيود: صفحة واحدة كحد أقصى (~200-250 كلمة)، من غير مصطلحات تقنية.
معيار النجاح: أول 3 أسطر لوحدها كافية لاتخاذ قرار.
```

**مخرج الأصلي (ملخص):** قالب تقرير عام فاضي من البيانات الفعلية (عناوين زي "مقدمة"، "الأداء المالي"، "التوصيات" بجمل عامة زي "حققت الشركة أداء جيد" من غير أرقام حقيقية). الطول ~320 كلمة.

**مخرج المحول (ملخص):** تقرير بعنوان "تقرير أداء المبيعات - يناير/مارس"، ملخص تنفيذي في 3 أسطر (باقة A مستقرة مع تباطؤ طفيف، باقة B نمو متسارع +25% ويستاهل استثمار تسويقي، والتوصية بمراجعة سعر A)، وبعده نقاط تفصيلية بالأرقام الفعلية. ~210 كلمة.

**الفرق:**
1. الأصلي هيكل فاضي من البيانات، المحول ربط كل نقطة برقم حقيقي.
2. الأصلي بلا ملخص تنفيذي واضح في الأول، المحول حط التوصية في أول 3 أسطر.
3. الأصلي أطول من حد الصفحة الواحدة، المحول التزم بالقيد.

---

## Prompt #6
**الأصلي:** ليه أرقام التقرير مش مظبوطة

**الناقص:** المدخلات (مفيش تقرير ولا مصدر بيانات نقارن بيه) هو الأهم، وبعده النطاق (مظبوطة مقارنة بإيه بالظبط؟).

**النسخة الكاملة:**
```
الدور: انت محلل بيانات بتعمل مراجعة جودة (QA) على تقرير قبل ما يتوزع.
السياق: التقرير المرفق بيقول إجمالي مبيعات يناير = 70,000 جنيه، لكن جدول المصدر [البيانات الأصلية] بيدي إجمالي فعلي = 73,000 جنيه (45,000 + 28,000).
المهمة: قارن الرقمين، وحدد مصدر الفرق، واقترح التصحيح.
الفورمات المطلوب: 3 أسطر بس - الفرق بالأرقام، السبب المحتمل، التصحيح المقترح.
القيود: من غير افتراضات عن أسباب مش مذكورة في البيانات.
معيار النجاح: نقدر نصحح الرقم في التقرير مباشرة من غير تحقيق إضافي.
```

**مخرج الأصلي (ملخص):** رد نظري عام بلا بيانات فعلية للمقارنة: "يمكن فيه خطأ جمع، أو تقريب، أو بيانات مفقودة، راجع المصدر" - تخمين عام مش مربوط بأرقام حقيقية.

**مخرج المحول (ملخص):** تشخيص دقيق: الفرق = 3,000 جنيه، السبب = التقرير حسب باقة A بس (45,000) ونسي يضيف باقة B (28,000)، التصحيح = تعديل الإجمالي لـ73,000 جنيه.

**الفرق:**
1. الأصلي تخمين عام، المحول تشخيص دقيق بمقارنة رقمين فعليين.
2. الأصلي بلا سبب محدد واحد، المحول حدد السبب الفعلي.
3. الأصلي بلا تصحيح رقمي، المحول قدّم الرقم الصحيح جاهز.

---

## Prompt #7
**الأصلي:** اكتب SQL يجيب أحسن العملاء

**الناقص:** تعريف "أحسن" (النطاق) هو الأهم، وبعده الـ schema (المدخلات) وصيغة المخرج.

**النسخة الكاملة:**
```
الدور: انت Backend Developer بتكتب استعلامات على MySQL 8.
السياق: جدول orders (id, customer_id, total_amount, created_at)، وجدول customers (id, name, email).
المهمة: اكتب استعلام يجيب أفضل 10 عملاء حسب إجمالي المبلغ المُنفق خلال آخر 3 شهور بس.
الفورمات المطلوب: SQL query جاهز للتشغيل بدون شرح.
القيود: استبعد الطلبات الملغاة لو فيه عمود status (لو مش موجود، تجاهل الشرط واذكر ده في تعليق).
معيار النجاح: الاستعلام يشتغل مباشرة على MySQL 8 من غير تعديل.
```

**مخرج الأصلي (ملخص):** `SELECT customer_id, SUM(total_amount) FROM orders GROUP BY customer_id ORDER BY SUM(total_amount) DESC LIMIT 10` - بلا فلترة على تاريخ آخر 3 شهور، بلا JOIN مع customers (الناتج IDs بس)، وبلا تعامل مع الطلبات الملغاة.

**مخرج المحول (ملخص):** استعلام كامل بـ JOIN مع customers، شرط `WHERE created_at >= DATE_SUB(CURDATE(), INTERVAL 3 MONTH)`، GROUP BY على اسم العميل، ORDER BY تنازلي، LIMIT 10 - جاهز للتشغيل مباشرة.

**الفرق:**
1. الأصلي يرجع IDs بس، المحول يرجع أسماء (JOIN فعلي).
2. الأصلي ياخد كل التاريخ، المحول التزم بفلتر آخر 3 شهور.
3. الأصلي بلا تعامل مع الطلبات الملغاة، المحول تعامل مع القيد صراحة.

---

## Prompt #8
**الأصلي:** حوّل الجدول ده لـ API كامل

**الناقص:** الدور، المهمة المحددة (CRUD كامل ولا read-only؟)، الفورمات (لغة/framework)، القيود (auth، pagination)، معيار النجاح.

**النسخة الكاملة:**
```
الدور: انت Backend Developer بتستخدم Laravel 11.
السياق: الجدول [أعلاه] هيتحول لجدول sales بـMySQL بالأعمدة: month, product, revenue, orders_count, new_customers.
المهمة: اعمل RESTful API كامل (CRUD) باستخدام Laravel API Resource Controller.
الفورمات المطلوب: Migration + Model + Controller (كل methods) + routes/api.php. كود بس بلا شرح.
القيود: Form Request للـvalidation، أخطاء بصيغة JSON:API، pagination في index (15 عنصر بالصفحة).
معيار النجاح: يشتغل مباشرة بعد php artisan migrate من غير تعديل.
```

**مخرج الأصلي (ملخص):** افترض framework مختلف (Express.js) بدل Laravel، عمل CRUD أساسي بلا validation حقيقي وبلا pagination، في ملف واحد كبير محتاج تعديل كامل للتظبيط على الـ stack بتاعنا.

**مخرج المحول (ملخص):** Laravel-specific فعلاً: migration بالأعمدة المطلوبة، Form Request بقواعد validation واضحة، Controller بـ5 methods مع pagination 15 وأخطاء JSON:API، وRoutes بـapiResource - جاهز للتشغيل تقريبًا.

**الفرق:**
1. الأصلي framework غير متوافق مع الـ stack، المحول التزم بـLaravel.
2. الأصلي بلا pagination ولا validation حقيقي، المحول عمل الاتنين.
3. الأصلي غير منظم ببنية Laravel، المحول فصّل كل جزء في مكانه الصحيح.

---

## Prompt #9
**الأصلي:** حوّل الشيت ده لـ JSON

**الناقص:** المدخلات (الشيت مش موجود أصلاً) هو الأهم، وبعده صيغة المخرج (بنية الـJSON) والقيود (أنواع البيانات، القيم الفاضية).

**النسخة الكاملة:**
```
الدور: انت مطور بتجهز بيانات لاستهلاكها من Frontend.
السياق: الشيت المرفق [جدول المبيعات أعلاه].
المهمة: حوّل كل صف لعنصر JSON.
الفورمات المطلوب: مصفوفة objects، مفاتيح بالإنجليزي: month, product, revenue, orders_count, new_customers. الأرقام numbers مش strings.
القيود: الخلية الفاضية ترجع null مش تتشال من الـobject.
معيار النجاح: JSON صحيح 100% وجاهز لملف .json مباشرة.
```

**مخرج الأصلي (ملخص):** سأل "أي شيت؟" أو -لو افترض بيانات وهمية- رجّع JSON بمفاتيح غير متسقة والأرقام strings ("45000" بدل 45000)، وبلا قاعدة واضحة للقيم الفاضية.

**مخرج المحول (ملخص):** array فيها 6 objects بالظبط بمفاتيح إنجليزي متسقة، الأرقام numbers فعلية، JSON valid بالكامل وجاهز مباشرة.

**الفرق:**
1. الأصلي محتاج توضيح أو بيانات وهمية، المحول اشتغل على البيانات الفعلية.
2. الأصلي أرقامه strings، المحول numbers زي المطلوب.
3. الأصلي بلا قاعدة للقيم الفاضية، المحول التزم بـnull بدل الحذف.

---

## Prompt #10
**الأصلي:** اشرح الـ pivot tables

**الناقص:** الجمهور المستهدف (مستوى الشرح) هو الأهم، وبعده النطاق (في أي برنامج؟) وصيغة المخرج.

**النسخة الكاملة:**
```
الدور: انت مدرب Excel بتشرح لموظف جديد في قسم المبيعات.
السياق: الموظف عنده خبرة أساسية (SUM وفلاتر بسيطة) بس أول مرة يسمع عن pivot tables، وعنده جدول المبيعات [أعلاه] وعايز يلخصه بسرعة.
المهمة: اشرح إيه هو الـpivot table وإزاي يستخدمه على البيانات دي عشان يطلع إجمالي المبيعات لكل منتج.
الفورمات المطلوب: خطوات مرقمة بلغة بسيطة + مثال واحد تطبيقي على نفس الجدول.
القيود: بلا screenshots (نص بس)، أقصى 150 كلمة.
معيار النجاح: الموظف يعمل pivot table صح من أول مرة من غير ما يسأل.
```

**مخرج الأصلي (ملخص):** شرح نظري أكاديمي ("aggregation"، "cross-tabulation") بلا مثال تطبيقي مربوط ببيانات حقيقية. ~300 كلمة.

**مخرج المحول (ملخص):** 5 خطوات مرقمة بسيطة (حدد الجدول → Insert → PivotTable → اسحب "المنتج" لـRows و"المبيعات" لـValues → يطلع الإجمالي لكل منتج تلقائي)، بلغة عملية بلا مصطلحات، ~130 كلمة.

**الفرق:**
1. الأصلي نظري بمصطلحات، المحول عملي بخطوات فعلية.
2. الأصلي بلا مثال مربوط بالبيانات، المحول طبّق على نفس الجدول مباشرة.
3. الأصلي أطول من ضعف الحد المطلوب، المحول التزم بـ150 كلمة.
